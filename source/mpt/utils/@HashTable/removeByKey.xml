<?xml version="1.0" encoding="utf-8" ?>

<help>
	<function>removeByKey</function>
	
	<!-- one-line description of the function -->
	<info>
		Remove element from the table based on the key.
	</info>
	
	<syntax>t = removeByKey(h,key)</syntax>
	<syntax>t = h.removeByKey(key)</syntax>

	<!-- long description of the function -->
	<descr>
        Remove element from the table stored under given <tt>key</tt>. If the removal was successful,
        logical 0 is returned.
	</descr>
	<!-- now describe input arguments -->
	
	<!-- mark inputs as mandatory by providing the "required" attribute -->
	<input required="true">
		<name>h</name>
        <descr>HashTable object.</descr>
		<!-- any class name can be used, it can even be omitted -->
		<class name="HashTable"/>
	</input>

    <input required="true">
		<name>key</name>
        <descr>String or an integer array representing the key of the element to remove.</descr>
		<!-- any class name can be used, it can even be omitted -->
		<class name="char or double"/>
	</input>

	<!-- now describe output arguments -->
	<output>
		<name>t</name>
        <descr>Logical scalar that informs whether the element was removed (false) or not (true).</descr>
		<class name="logical"/>
	</output>

	<!-- we can provide multiple code examples -->
    <example>
        <descr>Construct empty table</descr>		
        <cmd>h = HashTable</cmd>
        <descr>Put 10 elements inside the table.</descr> 
        <cmd>for i=1:10, h.put([i, 20-i, 1+i],struct('data',[i,20-i,1+i])); end </cmd>		
        <descr> Remove the element with the key [5 15 6]</descr>
        <cmd> h.removeByKey([5 15 6]) </cmd>
	</example>

	<!-- multiple related function can be defined -->
	<related>HashTable</related>
    <related>put</related>
    <related>removeByIndex</related>
    <related>replaceByIndex</related>
    <related>replaceByKey</related>
	
	
	<!-- multiple authors can be defined -->
	<author macro="author_herceg"/>

	<!-- finally, the text of the license -->
	<license macro="GPL2"/>
</help>
